from tomobase.core import registers


class DeviceController:
    def __init__(self, protocol, address, req_port, reply_port):
        self.components = registers.Registry(str, object)
        self.protocol = protocol
        
    def 
        
        